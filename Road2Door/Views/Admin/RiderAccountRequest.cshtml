<!DOCTYPE html>
<html>
<head>
        <link href="/css/admin/riderAccountRequest.css" rel="stylesheet" / >
</head>
<body>
    @if (Model.Count==0)
    {
        <h2>No request</h2>
        // Code to execute if the property exists
    }

    @foreach (Rider r in Model)
    {

        <div class="box">
            <p>Rider ID: <span id="riderId">@r.Id</span></p>
            <p>Name: <span id="name">@r.Name</span></p>
            <p>Email: <span id="email">@r.Email</span></p>
            <p>CNIC: <span id="cnic">@r.Cnic</span></p>
            <p>Phone: <span id="phone">@r.Phone</span></p>
            <p>Account Status: <span id="accountStatus" style="background-color: @((r.Status == 1) ? "green" : "red")">@r.Status</span></p>
            <a href="#" onclick="showLicenseModal('@Url.Content("~/uploads/Licnese/")' + '@r.License')">View Driving License</a>
            <a href="#" onclick="showLicenseModal('@Url.Content("~/uploads/CriminalRecord/")' + '@r.PoliceRecord')">View Police Record</a>
            <button onclick="acceptRequest(@r.Id)">Accept Request</button>
            <button onclick="rejectRequest(@r.Id)">Reject Request</button>
        </div>



    }


    <div id="licenseModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="hideLicenseModal()">&times;</span>
            <iframe id="licenseImage" src="" alt="Image not found" width="90%" height="90%"></iframe>
        </div>
    </div>

</body>

<script src="/js/admin/riderAccountRequest.js"></script>


</html>
